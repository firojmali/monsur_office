// import { createRouter, createWebHistory } from 'vue-router'
// import '@/functions/interfeces'
// import { useUserItemStore } from '../store/userItem'
// import routes from './routes'
// const userstore = useUserItemStore()
// const routeactive = []
// if (userstore.user != null && userstore.is_login()) {
//   const userRoles = userstore.getUserRoles()
//   routes.forEach((route) => {
//     if (
//       userRoles.find((role) => {
//         if (route.role) return route.role.find((routerole) => (role = routerole))
//         else return false
//       })
//     )
//       routeactive.push(route)
//   })
// }
// export default routeactive
